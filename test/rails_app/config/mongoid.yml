defaults: &defaults
  allow_dynamic_fields: true
  raise_not_found_error: true

test:
  sessions:
    default:
      database: devise_invitable-test-suite
      hosts:
        - localhost:27017
      options:
        <<: *defaults
